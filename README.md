# KasirQuh Customer QP

Project Next.js minimal untuk menjalankan source `app/`, `components/`, dan `lib/` yang ada di paket QP.

## Jalankan di SPCK NodeJS

```bash
npm install
npm run dev
```

Buka alamat localhost yang ditampilkan oleh Next.js menggunakan browser/DevSurf.
