'use client';
export default function ProductCard({produk,onAddToCart,onOpenDetail}){const stok=Number(produk.stok||0),harga=Number(produk.hargaJual||produk.harga||0),satuan=produk.satuan||'pcs';return <article className="modern-product-card" onClick={()=>onOpenDetail?.(produk)}><div className="product-stage">{stok<=0&&<span className="soldout">HABIS</span>}{stok>0&&stok<=5&&<span className="stock-badge">🔥 Sisa {stok}</span>}<div className="plate"/><img src={produk.foto||'/placeholder.svg'} alt={produk.nama||'Produk'} loading="lazy"/><button className="modern-cart-btn" disabled={stok<=0} onClick={e=>{e.stopPropagation();onAddToCart(produk)}}>🛒</button></div><div className="product-info"><b>{produk.nama||'Tanpa nama'}</b><strong>Rp {harga.toLocaleString('id-ID')}</strong><small>Stok: {stok} {satuan}</small></div></article>}
