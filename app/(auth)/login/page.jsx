'use client';import{useState}from'react';import{signIn}from'../../../lib/services/authService';export default function Login(){const[e,setE]=useState(''),[p,setP]=useState(''),[err,setErr]=useState('');return <main className="auth"><form className="card authbox" onSubmit={async x=>{x.preventDefault();try{await signIn(e,p);location.href='/customer'}catch(a){setErr(a?.message||'Login gagal')}}}><h1>Masuk</h1>{err&&<p>{err}</p>}<input className="input" placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><br/><br/><input className="input" type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)}/><br/><br/><button className="btn primary">Masuk</button> <a className="btn" href="/register">Daftar</a></form></main>}
